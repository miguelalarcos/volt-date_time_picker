require 'spec_helper'

describe Volt::DateTimePicker do
  it 'should have a version number' do
    expect(Volt::DateTimePicker::VERSION).not_to be nil
  end

  it 'should do something useful' do
    expect(false).to be true
  end
end
